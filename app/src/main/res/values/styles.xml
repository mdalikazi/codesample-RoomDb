<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@android:color/secondary_text_light</item>
        <item name="android:textColorSecondary">@android:color/tertiary_text_light</item>
        <!-- Required for support library -->
        <item name="android:textColorTertiary">@android:color/secondary_text_light</item>
    </style>

    <style name="CustomEmptyListText">
        <item name="android:layout_centerInParent">true</item>
        <item name="android:textColor">?android:textColorSecondary</item>
        <item name="android:textSize">20sp</item>
    </style>

</resources>
